# Song-Lyrics

A small Python-based project to fetch and display song lyrics for a given artist and song title.  
Useful as a demo of working with lyrics APIs, practicing HTTP requests, and building a simple command-line / web-based lyrics fetcher.

## 🎯 Purpose & Features

- Accepts user input: artist name and song title.  
- Fetches matching song lyrics from an online lyrics API.  
- Displays lyrics neatly in console or writes to a text file.  
- (Optional) Could be extended to a simple GUI or web-frontend for user-friendly lyrics lookup.  

## 🧰 Tech Stack

- **Python 3** — main scripting language  
- (Optional) Standard libraries such as `requests` — for making API calls, and `json` or `re` for parsing results  
- (Optional) `datetime`, `os`, or file-handling libraries — for logging or saving lyrics
